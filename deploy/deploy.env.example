# 复制成 deploy.env 再填写；deploy.env 不入库
# SSH 主机：~/.ssh/config 里的别名，或 user@ip
DEPLOY_HOST=my-server
# 对外访问地址，用于部署后的健康检查
PUBLIC_URL=https://1-2-3-4.sslip.io:8443
# 部署前后核对进程号不变的服务（同机的其他服务，比如 VPN），空格分隔，可留空
WATCH_SERVICES=
